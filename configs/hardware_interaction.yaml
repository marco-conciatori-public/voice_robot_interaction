# main_thread.py passes its own verbosity in as a keyword argument, which wins over this value. It is
# still needed here so that constructing HardwareInteraction on its own (a script, a test) works
# without it, which is what the zero-argument IDE run relies on everywhere else.
verbose: 0

bus_address: 0x15
smbus_id: 1
